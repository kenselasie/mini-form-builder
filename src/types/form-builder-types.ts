export interface Field {
  id: number;
  label: string;
  type: string;
  value: string;
}

export interface CustomiseFormDataType {
  backgroundColor: string;
  fontFamily: string;
  formLabels: Boolean;
}
